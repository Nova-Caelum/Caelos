# Composer reply · revision 05

Model/reasoning and reply-format popovers are an explicit exception to the above-composer placement rule: their top edges align with the outside bottom edge of the writing surface. This applies to compact hover panels, labeled click panels, and model/reasoning choice menus. The Add menu stays above. Tooltip positioning and lettering are unchanged.

The shared anchor measures the appropriate edge as the composer grows; transparent pointer bridges continue to join the controls to their panels. Radix choice menus retain collision handling.

Verified in the local browser: model and reply panels align exactly with the bottom edge; nested model/reasoning menus are within 0.35px. Reply selector fits inside the horizontal bounds of the 390px-wide, 276px-tall eight-line composer. Add remains above within 0.35px. Screenshot reviewed. Keyboard opening and Escape dismissal exercised; hover shares the same positioning code but pointer travel was not automated. Vite production build passed (2088 modules). Runtime and durable source copies match.
